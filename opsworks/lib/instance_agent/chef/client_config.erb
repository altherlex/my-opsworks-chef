# encoding: UTF-8
Ohai::Config[:plugin_path] << "<%= config[:ohai_plugin_path] %>"

local_mode <%= config[:local_mode] %>
cookbook_path <%= cookbook_path %>
file_cache_path "<%= config[:file_cache_path] %>"
node_path "<%= config[:search_nodes_dir] %>"
data_bag_path "<%= config[:data_bags_dir] %>"
log_level <%= config[:log_level] %>

%w{BUNDLE_GEMFILE GEM_HOME GEM_PATH RUBYLIB RUBYOPT}.each do |env|
  ENV[env] = nil
end

ENV['PATH'] = "/usr/local/bin:/usr/local/sbin:#{ENV['PATH']}"

File.umask 022
